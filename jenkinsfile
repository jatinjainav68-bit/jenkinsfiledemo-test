pipeline {
    agent any
    
    stages{
        stage ('stage1') {
            steps {
	          echo "hello this is first stage"
                 }
               }
        stage ('stage2') { 
            steps {
                 echo "hello this is second stage"
                 }
               }
	  }
}
